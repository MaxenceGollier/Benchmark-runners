name: Run Uno Benchmark

# Manual only, deliberately (this lives in the paper repo, not
# Penelopt.jl's own CI, and isn't meant to run on every push/PR).
on:
  workflow_dispatch:

jobs:
  Benchmark-Uno-Exact-Hessian:
    strategy:
      fail-fast: false
      matrix:
        split: [1, 2]

    runs-on: windows-latest

    env:
      CUTEST_SPLIT: ${{ matrix.split }}

    steps:
      - uses: actions/checkout@v4

      - uses: julia-actions/setup-julia@latest
        with:
          version: '1'

      - name: Use Julia cache
        uses: julia-actions/cache@v2

      - name: Instantiate project
        run: |
          julia --project=code/benchmarks/Uno -e '
            using Pkg
            Pkg.instantiate()
          '

      - name: Run Uno exact-Hessian benchmark
        run: |
          julia --project=code/benchmarks/Uno -e '
            include("code/benchmarks/Uno/benchmark-cutest-uno-exact-hessian.jl")
          '

      - name: Upload Uno exact-Hessian benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: cutest-uno-benchmark-stats-exact-hessian-${{ matrix.split }}
          path: code/benchmarks/Uno/result
          if-no-files-found: error

  Certify-Uno:
    needs: [Benchmark-Uno-Exact-Hessian]

    runs-on: windows-latest

    # Same rationale as Penelopt.jl's Certify-Ipopt: only run once every
    # split has produced results, so PRs/reruns load what this produced
    # instead of re-solving flagged problems every time.
    if: needs.Benchmark-Uno-Exact-Hessian.result == 'success'

    steps:
      - uses: actions/checkout@v4

      - uses: julia-actions/setup-julia@latest
        with:
          version: '1'

      - name: Use Julia cache
        uses: julia-actions/cache@v2

      - name: Instantiate project
        run: |
          julia --project=code/benchmarks/Uno -e '
            using Pkg
            Pkg.instantiate()
          '

      - name: Download benchmark results
        uses: actions/download-artifact@v4
        with:
          pattern: cutest-uno-benchmark-stats-exact-hessian-*
          path: code/benchmarks/Uno/result
          merge-multiple: true

      - name: Certify infeasibility
        run: |
          julia --project=code/benchmarks/Uno -e '
            include("code/benchmarks/Uno/certify-infeasibility.jl")
          '

      - name: Upload infeasibility certification
        uses: actions/upload-artifact@v4
        with:
          name: infeasibility-certification-uno
          path: code/benchmarks/Uno/result/infeasibility_certification.jld2
          if-no-files-found: error
